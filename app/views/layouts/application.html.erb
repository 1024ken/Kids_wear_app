<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>KIDS WEAR</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= Gon::Base.render_data({}) %>
  </head>

  <body>
    <!--Navgation-->
    <nav class="navbar navbar-default adjust navbar-fixed-top">
      <div class="container-fluid">
        
        <!--Brand and Toggle-->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#gnavi">
            <span class="sr-only"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a href="/" class="navbar-brand">KIDS WEAR</a>
        </div>
        
    
    <!--メニューリスト-->
      <div id="gnavi" class="collapse navbar-collapse">
        <ul class="nav navbar-nav navbar-right">
          <!--ログイン状態-->
          <% if logged_in? %>
            <% if current_user.children.empty? %>
              <li><%= link_to "はじめよう",top_children_question_children_path(current_user.id),class:"btn btn-default btn-lg" %></li>
            <% end %>
              <li><%= link_to "マイページ", users_path(current_user.id) %></li>
              <li><%= link_to "ログアウト", session_path(current_user.id), method: :delete %></li>
          <% else %>
          <!--未ログイン-->
            <li><%= link_to "新規登録", new_user_path %></li>
            <li><%= link_to "ログイン", new_session_path %></li>
          <% end %>
        </ul>
      </div>
      <!--/.navbar-collapse-->
    </div>
    <!--/.container-->
  </nav>
  
  
    <% if flash[:notice] %>
  　  <div class="alert alert-success">
       <%= flash[:notice] %>
     </div>
    <% end %>
    
<%= yield %>


  <!--<footer>-->
  <!--  <small>© 2017 Copyright KIDS WEAR powered by K.O.</small>-->
  <!--</footer>-->
</body>
</html>
